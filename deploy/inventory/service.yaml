apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: optiscale-demo
  labels:
    app: inventory-service
spec:
  type: ClusterIP
  selector:
    app: inventory-service
  ports:
    - name: http
      port: 8080
      targetPort: http
